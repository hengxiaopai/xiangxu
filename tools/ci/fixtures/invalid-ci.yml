name: Invalid CI fixture

on:
  pull_request_target:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - continue-on-error: true
        run: npm install
